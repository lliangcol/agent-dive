# Graphify Learning Plan

## Learning Objectives

- Understand how Graphify converts project materials into knowledge graphs.
- Run through the minimum map construction once.
- Understand Codex/Claude Code integration methods and write boundaries.
- Source code confirmation CLI, extraction, graph construction, export, and key call chain of MCP server.
- Summarize its reference value for Agentic Coding and contextual engineering.

## Stage arrangement

1. Understand the project: read the README, docs, and security notes.
2. Run through: install `graphifyy`, and execute `graphify .` on the public small repository.
3. Source reading: Enter from `graphify.__main__:main` and `graphify.serve:_main`.
4. Integration verification: test Codex project install and MCP server.
5. Summary evaluation: Compare codebase-memory-mcp, ordinary RAG and grep workflow.

## Validate boundaries

- Do not write "available" as a fact before completing the local operation.
- Do not write the README process as a real call chain before reading the source code.
- Before checking `SECURITY.md` and README Privacy, semantic extraction will not be performed on non-code materials such as docs, PDFs, and images in private repositories.

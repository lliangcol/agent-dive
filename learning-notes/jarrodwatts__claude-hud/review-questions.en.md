# Claude HUD Review Questions

1. Claude HUD Why choose Claude Code statusline instead of TUI alone?
2. What is the main call chain of `src/index.ts`?
3. `readStdin()` How to avoid getting stuck when there is no stdin?
4. What is the difference between the native value and the fallback value of context percent?
5. How to identify tool, Agent, and Todo in transcript JSONL?
6. Why can’t the completion time of background Agent only look at tool_result?
7. Which fields in `config.json` will affect the expanded layout?
8. Why does setup detect existing statusline and create a backup?
9. Why can’t the statusLine command of Windows PowerShell and Git Bash be mixed?
10. Why is subscriber usage not guaranteed to be visible to API-key-only users?
11. What are the restrictions on the path and freshness of external usage snapshot?
12. How to group and locate the current Windows `npm test` failure?

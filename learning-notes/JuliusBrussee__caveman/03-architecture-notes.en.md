# Caveman Architecture Notes

To be filled in.

Suggested records:

- `bin/install.js`’s provider matrix and installation strategy.
- The relationship between skill source files and plugin / distribution mirror.
- Data flow for Claude Code hooks and mode flags.
- MCP JSON-RPC passthrough boundary for `caveman-shrink`.
- File read and write, backup and model call boundaries for `caveman-compress`.
- Input, output and statistical calibers of benchmark / eval.

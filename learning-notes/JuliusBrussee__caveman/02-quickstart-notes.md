# Caveman 快速开始笔记

*(内容待补，在学习阶段填写。)*

建议记录：

- Node / npm / npx 版本。
- 固定的仓库 commit。
- `node bin/install.js --list` 输出摘要。
- `node bin/install.js --dry-run --all` 输出摘要。
- 实际选择的单 Agent 安装路径。
- 激活、关闭、卸载命令。
- 遇到的 PowerShell、PATH、权限或配置合并问题。

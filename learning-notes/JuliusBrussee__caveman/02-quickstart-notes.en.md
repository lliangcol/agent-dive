# Caveman Quickstart notes

To be filled in.

Suggested records:

- Node/npm/npx version.
- Fixed repository commit.
- `node bin/install.js --list` Output summary.
- `node bin/install.js --dry-run --all` Output summary.
- The actual selected single-Agent installation path.
- Activate, close, and uninstall commands.
- PowerShell, PATH, permissions, or configuration merge issues encountered.

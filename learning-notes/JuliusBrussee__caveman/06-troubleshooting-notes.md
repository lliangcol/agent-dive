# Caveman 问题排查笔记

待填写。

建议记录：

- 安装器无法检测目标 Agent。
- Node / npx / PowerShell 执行问题。
- dry-run 与实际写入不一致。
- 既有配置合并失败。
- Claude Code hooks 未激活或 statusline 不显示。
- Codex 中 `/caveman` 未触发。
- MCP shrink 包装后 server 不响应。
- `caveman-compress` 备份、权限或模型调用失败。

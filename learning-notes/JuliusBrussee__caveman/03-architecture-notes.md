# Caveman 架构笔记

待填写。

建议记录：

- `bin/install.js` 的 provider matrix 和安装策略。
- skill 源头文件与 plugin / distribution mirror 的关系。
- Claude Code hooks 和 mode flag 的数据流。
- `caveman-shrink` 的 MCP JSON-RPC 透传边界。
- `caveman-compress` 的文件读写、备份和模型调用边界。
- benchmark / eval 的输入、输出和统计口径。

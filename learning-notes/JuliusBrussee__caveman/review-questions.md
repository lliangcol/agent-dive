# Caveman 复习问题

1. Caveman 与普通 “be concise” system prompt 的核心差异是什么？
2. `bin/install.js` 如何决定安装到哪些 Agent？
3. 为什么学习 Caveman 必须先验证 dry-run 写入范围？
4. Claude Code SessionStart hook 和 UserPromptSubmit hook 分别负责什么？
5. mode flag 在 hook 和 statusline 之间承担什么角色？
6. Codex 路径和 Claude Code 路径的激活机制有什么不同？
7. `caveman-shrink` 应该压缩哪些字段，不应该改写哪些数据？
8. `caveman-compress` 为什么需要备份和文件大小限制？
9. README 中的 token reduction 数据为什么需要复现口径？
10. 哪些场景不适合使用 ultra 或极端压缩模式？

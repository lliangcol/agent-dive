# Ruflo 快速开始笔记

## 计划验证环境

- 操作系统：待记录
- Node.js：待记录，要求 `>=20`
- npm：待记录
- Ruflo 版本：待记录

## 待执行命令

```bash
npx --yes ruflo@latest --version
npx --yes ruflo@latest --help
npx --yes ruflo@latest mcp start --help
npx --yes ruflo@latest init --help
npx --yes --package @claude-flow/cli@latest claude-flow --version
```

## 记录模板

| 命令 | 是否成功 | 耗时 | 是否写入文件 | stdout/stderr 摘要 | 备注 |
|---|---|---:|---|---|---|
| `npx --yes ruflo@latest --version` | 待执行 | - | 待确认 | - | - |
| `npx --yes ruflo@latest --help` | 待执行 | - | 待确认 | - | - |
| `npx --yes ruflo@latest mcp start --help` | 待执行 | - | 待确认 | - | - |
| `npx --yes ruflo@latest init --help` | 待执行 | - | 待确认 | - | - |

## 当前结论

尚未执行任何本机命令。README 和 npm metadata 只能说明预期路径，不能作为跑通证明。

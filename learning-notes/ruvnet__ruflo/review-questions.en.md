# Ruflo Review Questions

1. What are the relationships between Ruflo, Claude Flow, `@claude-flow/cli`, and `@claude-flow/codex`?
2. What is the difference in capabilities between Claude Code plugin lite and CLI full install?
3. Why can’t Claude Code hooks be inferred as Codex hooks?
4. `ruflo/bin/ruflo.js` How to distinguish between normal CLI mode and MCP mode?
5. What stability improvements has `@claude-flow/cli/bin/cli.js` done for MCP stdio?
6. `SwarmCoordinator` How to choose an agent to execute a task?
7. `Agent.executeTask()` How to handle failure?
8. What are the rollback conditions for `WorkflowEngine`?
9. `HybridBackend` Why use SQLite and AgentDB together?
10. In `.claude-plugin/hooks/hooks.json`, what does `|  | true` mean for security access control?
11. What Codex related capabilities are configured in `.agents/config.toml`?
12. Why does the number of capabilities in the README need to be reviewed?

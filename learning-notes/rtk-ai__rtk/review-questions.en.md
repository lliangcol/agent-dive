# RTK Review Questions

1. What is the core difference between RTK and ordinary shell aliases?
2. Why do hook scripts need to be thin delegates instead of copying the rewrite logic to each Agent?
3. Why can’t Codex integration be equal to Claude Code transparent hook?
4. What risk does `Deny > Ask > Allow > Default`’s permission priority address?
5. Under what circumstances should raw output be read back instead of just looking at RTK filtered output?
6. What problems do `RTK_TEE_DIR` and `RTK_TELEMETRY_DISABLED` solve respectively?
7. How to verify the token savings conclusion in the README?
8. If you want to add a new command filter, which modules and tests should you read first?

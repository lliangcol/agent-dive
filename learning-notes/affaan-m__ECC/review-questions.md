# ECC 复习问题

1. ECC 为什么不是普通 prompt library？
2. `skills/` 为什么比 `commands/` 更适合作为长期 workflow source？
3. Codex 路径为什么不能假设 Claude-style hooks 会执行？
4. `ecc plan` 和 `ecc install` 的风险差异是什么？
5. minimal、core、full profile 应该如何验证？
6. Hook lifecycle 中 PreToolUse、PostToolUse、Stop、SessionStart 各适合做什么？
7. 安装器为什么需要 install-state？
8. 如何判断一个 MCP config 是否适合默认启用？
9. ECC 的 operator tools 解决哪些长期 agent 工作问题？
10. 在团队仓库中引入 ECC 前必须做哪些安全检查？

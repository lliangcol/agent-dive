# ECC Review Questions

1. Why is ECC not a normal prompt library?
2. Why is `skills/` more suitable as a long-term workflow source than `commands/`?
3. Why can’t Codex paths assume that Claude-style hooks will be executed?
4. What is the risk difference between `ecc plan` and `ecc install`?
5. How should minimal, core and full profiles be verified?
6. What are PreToolUse, PostToolUse, Stop, and SessionStart each suitable for in Hook lifecycle?
7. Why does the installer need install-state?
8. How to determine whether an MCP config is suitable to be enabled by default?
9. What long-term agent work problems are solved by ECC’s operator tools?
10. What security checks must be done before introducing ECC in a team repository?

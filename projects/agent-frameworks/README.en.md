# Agent Frameworks

Common Agent framework collect area. Suitable for saving projects that provide Agent Loop, tool invocation, state management, workflow or application framework capabilities.

The project directory is named `owner__repo`.
